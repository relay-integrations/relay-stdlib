{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "template": {
      "type": "string",
      "description": "The template to execute in Go's text/template syntax"
    },
    "data": {
      "type": "object",
      "description": "The input data to provide to the template"
    }
  },
  "required": [
    "template",
    "data"
  ],
  "additionalProperties": false
}
